const urlToHashmap = require('../urlToHashmap.js');

describe('Basic test of urlToHashmap', () => {
    it('should import urlToHashmap', () => {
        expect(urlToHashmap()).not.toBeNull();
    });
});

